/**
 * Platform agent constants: cache file prefix, API path placeholders.
 * Real API base URLs / paths are resolved inside platform adapters (not exposed to config).
 */

/** Cache file name prefix; full name is `.platform-agent-cache-<platformType>.json`. */
export const PLATFORM_AGENT_CACHE_PREFIX = ".platform-agent-cache-"
