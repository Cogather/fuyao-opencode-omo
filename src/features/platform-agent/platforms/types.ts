/**
 * Platform adapter types. Re-export from parent for optional use under platforms/.
 * Primary definitions live in features/platform-agent/types.ts.
 */

export type {
  PlatformType,
  PlatformAgentApp,
  GetAgentListOptions,
  GetAgentDetailOptions,
  PublishResult,
  IPlatformAdapter,
} from "../types"
